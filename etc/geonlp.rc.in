; 地名語抽出システム
; プロファイルサンプル
;

; GeonlpMA::parse() の戻り値のフォーマットの指定。
; DefaultGeowordFormatterあるいはChasenGeowordFormatterのいずれかを指定する。
; DefaultGeowordFormatterはMeCabのデフォルトフォーマット、
; ChasenGeowordFormatterはchasenフォーマットにそれぞれ準拠したもの。
formatter = DefaultGeowordFormatter
; formatter = ChasenGeowordFormatter

; 地名接尾辞集合の定義。地名接尾辞は、表層形、読み、発音をカンマで区切って示す。
; 複数ある場合にはこれを'|'で区切って記す。
suffix = 前,マエ,マエ|内,ナイ,ナイ|立,リツ,リツ|境,サカイ,サカイ|東,ヒガシ,ヒガシ|西,ニシ,ニシ|南,ミナミ,ミナミ|北,キタ,キタ

; 空間語の定義。空間語は、品詞に関わらず地名語の後に続くことができる。
; 複数ある場合にはこれを'|'で区切って記す。
spatial = 付近|周辺|周囲|経由|沿線|沿岸|沿い|沖|上流|下流|河口|出身|地区|－|間|地|発|着

; 利用する辞書のリスト
; 辞書ID（整数）を列挙する。省略した場合は全辞書が利用される。
; ただし "-" で始まる表記に一致するテーマは除外される。
dictionary = 

; 利用する固有名クラスのリスト
; 正規表現で記述し、順番に評価する。いずれかに一致すれば利用する。
; ただし "-" で始まる表記に一致するテーマは除外される。
ne_class =

; 住所要素とみなされる固有名クラスの正規表現
; 正規表現で記述する（リストではない）
address_regex = ^(都道府県|市区町村|行政地域|居住地名)(\/.+|)

; データファイルディレクトリ
; このディレクトリ下に各種データや辞書が配置される
data_dir = @prefix@/lib/geonlp

; ログファイルディレクトリ
; このディレクトリ下にログファイルが出力される
log_dir  = @prefix@/lib/geonlp/log

; 住所ジオコーダ DAMS の辞書ファイルパス
; 省略した場合は DAMS インストールのデフォルト値が利用される
; 通常は設定不要
; dams_path = /usr/local/lib/dams/dams
